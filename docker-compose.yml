version: "3.9"

# ─── Networks (triangle topology) ─────────────────────────────────────────────
networks:
  net_ab:
    driver: bridge
    ipam:
      config:
        - subnet: 10.0.1.0/24
          gateway: 10.0.1.254

  net_bc:
    driver: bridge
    ipam:
      config:
        - subnet: 10.0.2.0/24
          gateway: 10.0.2.254

  net_ac:
    driver: bridge
    ipam:
      config:
        - subnet: 10.0.3.0/24
          gateway: 10.0.3.254

# ─── Routers ──────────────────────────────────────────────────────────────────
services:

  router_a:
    build: .
    container_name: router_a
    privileged: true        # needed for 'ip route' kernel calls
    environment:
      MY_IP: "10.0.1.1"
      # Neighbors: Router B on net_ab, Router C on net_ac
      NEIGHBORS: "10.0.1.2,10.0.3.2"
    networks:
      net_ab:
        ipv4_address: 10.0.1.1
      net_ac:
        ipv4_address: 10.0.3.1

  router_b:
    build: .
    container_name: router_b
    privileged: true
    environment:
      MY_IP: "10.0.1.2"
      # Neighbors: Router A on net_ab, Router C on net_bc
      NEIGHBORS: "10.0.1.1,10.0.2.2"
    networks:
      net_ab:
        ipv4_address: 10.0.1.2
      net_bc:
        ipv4_address: 10.0.2.1

  router_c:
    build: .
    container_name: router_c
    privileged: true
    environment:
      MY_IP: "10.0.2.2"
      # Neighbors: Router B on net_bc, Router A on net_ac
      NEIGHBORS: "10.0.2.1,10.0.3.1"
    networks:
      net_bc:
        ipv4_address: 10.0.2.2
      net_ac:
        ipv4_address: 10.0.3.2
